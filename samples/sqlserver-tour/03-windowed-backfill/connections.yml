erp:
  connector: sqlserver
  host: ${PZ_MSSQL_HOST}
  port: 14333
  database: ${PZ_MSSQL_DB}
  user: ${PZ_MSSQL_USER}
  password: ${PZ_MSSQL_PASSWORD}
  trust_server_certificate: true
  # The window (initial/max_window/until) is declared in the pipeline's WHERE,
  # so there is no entities block here at all.

mart:
  connector: sqlserver
  host: ${PZ_MSSQL_HOST}
  port: 14333
  database: ${PZ_MSSQL_DB}
  user: ${PZ_MSSQL_USER}
  password: ${PZ_MSSQL_PASSWORD}
  trust_server_certificate: true
