bin/
obj/
.pz/
# Sample projects write their outputs in place; run artifacts must never show up as untracked
# noise or get committed.
samples/*/out/
*.user
.superpowers/
BenchmarkDotNet.Artifacts/
artifacts/
# Rust SDK (rust/pz-connector): build output only -- Cargo.lock is committed (this workspace builds
# binaries, not a library published to crates.io, so the lockfile is part of what makes a build
# reproducible).
rust/target/
