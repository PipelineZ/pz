pg:
  connector: localfiles
  root: data
  entities:
    orders:
      read:
        path: orders.parquet
        format: parquet
    items:
      read:
        path: items.parquet
        format: parquet

lake:
  connector: localfiles
  root: out
