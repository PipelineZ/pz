<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- Internal assembly: ships inside the Pz.Cli tool package rather than as its own NuGet. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DuckDB.NET.Data.Full" Version="1.5.5" />
    <PackageReference Include="Apache.Arrow" Version="23.0.0" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Pz.DuckDb.Tests" />
    <InternalsVisibleTo Include="Pz.Engine.Tests" />
    <!-- SqlDryCompiler (tier 4) reuses ArrowInterop.QuoteQualified for identifier quoting rather than
         duplicating it; see src/Pz.Engine/Validation/SqlDryCompiler.cs. -->
    <InternalsVisibleTo Include="Pz.Engine" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Pz.Connectors.Abstractions\Pz.Connectors.Abstractions.csproj" />
    <ProjectReference Include="..\Pz.Core\Pz.Core.csproj" />
  </ItemGroup>

</Project>
